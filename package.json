{
  "name": "idle-party-rpg",
  "version": "0.1.0",
  "description": "Idle Party RPG with hexagonal world map",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "phaser": "^3.80.1"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "vite": "^5.4.2"
  }
}
