import { TileType, TILE_CONFIGS } from '@idle-party-rpg/shared';

console.log('Game Manager - placeholder');
console.log(`Available tile types: ${Object.values(TileType).join(', ')}`);
console.log(`Tile configs loaded: ${Object.keys(TILE_CONFIGS).length}`);
